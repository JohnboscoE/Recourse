# Secrets — never commit these
.env
.dev.vars
worker/.dev.vars
.env.local
*.key

# Node
node_modules/

# Build output
dist/
build/

# Cloudflare Workers local state + build artifacts
.wrangler/
worker/.wrangler/

# Foundry (contracts/)
contracts/out/
contracts/cache/
contracts/broadcast/
# contracts/lib/ is tracked via git submodules (see .gitmodules) for reproducible deps.

# TS
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db
