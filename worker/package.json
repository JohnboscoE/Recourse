{
  "name": "@recourse/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute recourse --file=./schema.sql --remote"
  },
  "dependencies": {
    "@recourse/shared": "workspace:*",
    "hono": "^4.6.3",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.99.0"
  }
}
